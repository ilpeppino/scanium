***REMOVED*** Grafana Alerting - Contact Points Provisioning
***REMOVED*** These are placeholder contact points - configure secrets via environment variables
***REMOVED***
***REMOVED*** REQUIRED CONFIGURATION (set via docker-compose environment variables):
***REMOVED*** - SMTP: GF_SMTP_* variables for email notifications
***REMOVED*** - Webhook: Update the webhook URL in Grafana UI or use a secret manager
***REMOVED***
***REMOVED*** See monitoring/README.md for configuration instructions.

apiVersion: 1

contactPoints:
  ***REMOVED*** Default contact point for all environments
  - orgId: 1
    name: scanium-default
    receivers:
      ***REMOVED*** Webhook receiver (placeholder - update URL in UI or via secrets)
      - uid: scanium-webhook
        type: webhook
        settings:
          url: "${SCANIUM_ALERT_WEBHOOK_URL:-http://localhost:8080/alerts}"
          httpMethod: POST
          maxAlerts: 10
        disableResolveMessage: false

  ***REMOVED*** High priority contact point for production alerts
  - orgId: 1
    name: scanium-high-priority
    receivers:
      ***REMOVED*** Primary webhook for production
      - uid: scanium-webhook-prod
        type: webhook
        settings:
          url: "${SCANIUM_ALERT_WEBHOOK_URL_PROD:-http://localhost:8080/alerts/prod}"
          httpMethod: POST
          maxAlerts: 5
        disableResolveMessage: false
      ***REMOVED*** Email fallback (requires SMTP configuration)
      ***REMOVED*** Uncomment and configure GF_SMTP_* environment variables
      ***REMOVED*** - uid: scanium-email-prod
      ***REMOVED***   type: email
      ***REMOVED***   settings:
      ***REMOVED***     addresses: "${SCANIUM_ALERT_EMAIL:-alerts@example.com}"
      ***REMOVED***     singleEmail: true
      ***REMOVED***   disableResolveMessage: false

  ***REMOVED*** Low priority contact point for dev/staging
  - orgId: 1
    name: scanium-low-priority
    receivers:
      - uid: scanium-webhook-dev
        type: webhook
        settings:
          url: "${SCANIUM_ALERT_WEBHOOK_URL_DEV:-http://localhost:8080/alerts/dev}"
          httpMethod: POST
          maxAlerts: 20
        disableResolveMessage: false
