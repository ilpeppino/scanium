package com.scanium.core.tracking

/**
 * Placeholder file for KMP module skeleton.
 *
 * This module will contain platform-independent tracking and aggregation logic:
 * - ObjectTracker (multi-frame tracking with spatial matching)
 * - ObjectCandidate (candidate state during tracking)
 * - TrackerConfig (tunable thresholds)
 * - ItemAggregator (session-level similarity deduplication)
 * - AggregationPresets (configuration presets)
 * - AggregatedItem (merged detection with metadata)
 * - Logger (platform-agnostic logging interface with expect/actual)
 * - DetectionInfo (input to tracker)
 *
 * STRICT RULE: No android.* or androidx.* imports allowed in commonMain.
 * DEPENDENCY: Depends on shared:core-models only.
 */
internal object Placeholder
