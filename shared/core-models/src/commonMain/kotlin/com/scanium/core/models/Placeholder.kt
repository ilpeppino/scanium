package com.scanium.core.models

/**
 * Placeholder file for KMP module skeleton.
 *
 * This module will contain platform-independent data models:
 * - ImageRef (platform-agnostic image reference)
 * - NormalizedRect (normalized bounding box)
 * - ScannedItem (immutable item model)
 * - DetectionResult (real-time detection result)
 * - RawDetection (raw ML detection data)
 * - ItemCategory (detection category enum)
 * - ScanMode (scan mode enum)
 * - ClassificationMode (classification mode enum)
 *
 * STRICT RULE: No android.* or androidx.* imports allowed in commonMain.
 */
internal object Placeholder
