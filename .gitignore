# Gradle
/.gradle/
**/build/

# Android app module (keep sources tracked; ignore outputs only)
/androidApp/build/

# IDE & tooling
.idea/
*.iml
.DS_Store

# Android/NDK intermediates
/.cxx/
local.properties
captures/

# Signing / keys (none should be committed)
*.keystore
*.jks

# Logs and temp files
*.log
crash.log
tombstone_*

# Local-only Gradle overrides
/.gradle-local/

# Node
node_modules

# Local test logs / automation outputs
# Includes tmp/ci/ for local CI runner outputs (coverage, security reports)
tmp/

# Termux remote SSH config (contains personal SSH host/user)
scripts/termux/remote_env

# Backend environment files (secrets)
backend/.env
backend/.env.*
backend/.env.backup
backend/.env.last
!backend/.env.example

# NAS deployment environment files (secrets)
deploy/nas/env/*.env
!deploy/nas/env/*.env.example

# Docker images
*.tar.gz

# Monitoring data directories (local dev)
monitoring/data/
