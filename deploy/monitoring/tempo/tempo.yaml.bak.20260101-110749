server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        http:
        grpc:

ingester:
  max_block_duration: 5m

compactor:
  compaction:
    block_retention: 168h ***REMOVED*** 7 days

storage:
  trace:
    backend: local
    local:
      path: /var/var/tempo/traces
