# Scanium Monitoring (NAS) - example env file
# Copy to: deploy/nas/env/monitoring.env   (DO NOT COMMIT monitoring.env)

# Grafana admin login (set strong password on NAS)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=

# Optional: if you want to set a fixed Grafana URL (useful behind reverse proxy)
# GRAFANA_ROOT_URL=http://<NAS_LAN_IP>:3000
GRAFANA_ROOT_URL=
