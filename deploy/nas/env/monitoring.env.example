# =============================================================================
# Scanium Monitoring Stack - NAS Deployment Environment Variables
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp monitoring.env.example monitoring.env
# 2. Fill in values as needed (most have sensible defaults)
# 3. Keep monitoring.env private (it's gitignored)
# =============================================================================

# -----------------------------------------------------------------------------
# Grafana Configuration
# -----------------------------------------------------------------------------
# Admin credentials (CHANGE THESE for production!)
# Generate password with: openssl rand -base64 24
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD

# Disable anonymous access for production
GF_AUTH_ANONYMOUS_ENABLED=false
GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
GF_AUTH_DISABLE_LOGIN_FORM=false

# Server URL (adjust if exposing externally)
# LAN access: http://192.168.x.x:3000
GF_SERVER_ROOT_URL=http://localhost:3000

# Disable telemetry
GF_ANALYTICS_REPORTING_ENABLED=false
GF_ANALYTICS_CHECK_FOR_UPDATES=false

# Enable TraceQL editor for Tempo
GF_FEATURE_TOGGLES_ENABLE=traceqlEditor

# -----------------------------------------------------------------------------
# Data Retention (adjust based on NAS storage capacity)
# -----------------------------------------------------------------------------
# Loki (logs) - default 14 days
# LOKI_RETENTION_PERIOD=336h

# Tempo (traces) - default 7 days
# TEMPO_RETENTION_PERIOD=168h

# Mimir (metrics) - default 15 days
# MIMIR_RETENTION_PERIOD=15d

# -----------------------------------------------------------------------------
# Resource Hints
# -----------------------------------------------------------------------------
# Note: These are documented hints. Actual limits are set in docker-compose.
# DS418play has 2 cores (Celeron J3455) and 6GB RAM.
# Recommended allocation:
#   - Grafana: 256MB
#   - Alloy: 128MB
#   - Loki: 256MB
#   - Tempo: 256MB
#   - Mimir: 256MB
#   - Total: ~1.2GB (leaves room for other containers and system)

# -----------------------------------------------------------------------------
# Network Ports (for reference, configured in docker-compose)
# -----------------------------------------------------------------------------
# Grafana UI: 3000 (expose to LAN)
# Alloy OTLP HTTP: 4318 (expose to LAN for app telemetry)
# Alloy OTLP gRPC: 4317 (expose to LAN for app telemetry)
# Loki HTTP: 3100 (internal only)
# Tempo HTTP: 3200 (internal only)
# Mimir HTTP: 9009 (internal only)
# Alloy UI: 12345 (internal only, for debugging)
