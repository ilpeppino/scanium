# Scanium Backend (NAS) - example env file
# Copy to: deploy/nas/env/backend.env   (DO NOT COMMIT backend.env)

# Server
PORT=8080
NODE_ENV=production

# CORS (set to your app domains; keep permissive only for LAN dev)
CORS_ORIGIN=*

# Database (service name will be "postgres" in NAS compose)
# For NAS compose, this will usually be:
# DATABASE_URL=postgresql://scanium:scanium_password@postgres:5432/scanium?schema=public
DATABASE_URL=

# API key required by backend (if your backend enforces it)
# Set this to a random strong string on NAS; do not commit.
API_KEY=

# Optional: OpenTelemetry export to Alloy (if enabled)
# Example:
# OTEL_EXPORTER_OTLP_ENDPOINT=http://alloy:4318
OTEL_EXPORTER_OTLP_ENDPOINT=
